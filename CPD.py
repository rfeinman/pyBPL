#CPD for motor programs

class CPD(object):


	#functions used in generate_character

	def sample_number(libclass):

	def sample_relation_type(libclass,template.S[0:i]):

	def sample_sequence(libclass,ns): #gives template.S[i].ids

	def sample_shape_type(libclass,template.S[i].ids):
	
	def sample_invscales_type(libclass,template.S[i].ids):


	#functions used in generate_exemplar

	def sample_relation_token(libclass, M.S[i].R.eval_spot_type):

	def sample_position(libclass, M.S[i].R, M.S[1:i]): #check that this does what I want, slicewise
	
	def sample_shape_token(libclass, M.S[i].shapes_type):

	def sample_invscale_token(libclass, M.S[i].invscales_type):

	def sample_affine(libclass):

	def sample_image(M.pimg):